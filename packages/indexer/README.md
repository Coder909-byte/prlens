# packages/indexer

Not built yet - this is the Milestone 3 repo indexer (clone repo, tree-sitter parse, chunk by symbol, embed with Voyage, build call graph). Left as a placeholder folder so the monorepo layout matches CLAUDE.md.
